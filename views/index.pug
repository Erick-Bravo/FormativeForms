extends layout.pug

block content
  table
    thead: h2 Existing Users
      tr
        th ID
        th First Name
        th Last Name
        th Email
    tbody
      each user in users
        tr
          td #{user.id}
          td #{user.firstName}
          td #{user.lastName}
          td #{user.email}
